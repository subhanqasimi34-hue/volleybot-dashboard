body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #0D0D0D;
  color: #FFF;
  display: flex;
}

/* Sidebar */
.sidebar {
  width: 230px;
  background: #121212;
  height: 100vh;
  padding: 25px 20px;
  box-shadow: 0 0 20px rgba(168, 85, 247, 0.3);
}

.logo {
  color: #A855F7;
  text-align: center;
  margin-bottom: 40px;
}

.sidebar a {
  display: block;
  padding: 12px;
  margin-bottom: 10px;
  border-radius: 6px;
  color: #CFCFCF;
  cursor: pointer;
  transition: 0.15s;
}

.sidebar a:hover {
  background: rgba(59, 130, 246, 0.2);
  color: #FFF;
}

.sidebar .active {
  background: rgba(168, 85, 247, 0.25);
  color: #A855F7;
  box-shadow: 0 0 12px rgba(168, 85, 247, 0.5);
}

/* Main Content */
.main {
  padding: 30px;
  width: 100%;
}

.cards {
  display: flex;
  gap: 20px;
  margin-top: 25px;
}

.card {
  flex: 1;
  background: #1A1A1A;
  padding: 20px;
  border-radius: 12px;
  text-align: left;
  font-size: 18px;
  box-shadow: 0 0 10px rgba(255,255,255,0.05);
}

.card p {
  font-size: 28px;
  margin-top: 8px;
}

/* Neon Colors */
.purple {
  border-left: 4px solid #A855F7;
  box-shadow: 0 0 12px rgba(168, 85, 247, 0.45);
}

.blue {
  border-left: 4px solid #3B82F6;
  box-shadow: 0 0 12px rgba(59, 130, 246, 0.45);
}

.green {
  border-left: 4px solid #22C55E;
  box-shadow: 0 0 12px rgba(34, 197, 94, 0.45);
}

.cyan {
  border-left: 4px solid #06B6D4;
  box-shadow: 0 0 12px rgba(6, 182, 212, 0.45);
}

/* Chart placeholders */
.charts {
  display: flex;
  gap: 20px;
  margin-top: 40px;
}

.chart {
  flex: 1;
  background: #1A1A1A;
  padding: 25px;
  border-radius: 12px;
  text-align: center;
  font-size: 20px;
  opacity: 0.7;
  box-shadow: 0 0 10px rgba(255,255,255,0.05);
}
/* Premium Page */
.premium-container {
  margin-top: 40px;
  display: flex;
  justify-content: center;
}

.premium-card {
  background: #1A1A1A;
  padding: 30px;
  width: 400px;
  border-radius: 12px;
  text-align: center;
  box-shadow: 0 0 20px rgba(168,85,247,0.35);
}

.premium-title {
  color: #A855F7;
  margin-bottom: 10px;
}

.premium-price {
  font-size: 32px;
  margin: 20px 0;
  color: #3B82F6;
}

.premium-list {
  list-style: none;
  padding: 0;
  margin-bottom: 30px;
  text-align: left;
}

.premium-list li {
  margin: 10px 0;
  color: #CFCFCF;
}

.paypal-btn {
  width: 100%;
  padding: 15px;
  border: none;
  border-radius: 8px;
  font-size: 18px;
  cursor: pointer;
  background: #3b7bbf;
  color: white;
  box-shadow: 0 0 15px rgba(59,123,191,0.5);
  transition: 0.2s;
}

.paypal-btn:hover {
  background: #499eed;
  box-shadow: 0 0 22px rgba(59,123,191,0.8);
}


